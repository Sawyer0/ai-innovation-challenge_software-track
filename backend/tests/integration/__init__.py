"""
Integration tests - test component interactions.

These tests use the test database and may:
- Query test database
- Call internal services
- Test API endpoints via TestClient

Do NOT call external APIs (mocked).
"""
